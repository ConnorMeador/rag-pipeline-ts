# rag-pipeline-ts

A small TypeScript RAG (retrieval-augmented generation) pipeline, extracted
and adapted from a production system.

Work in progress — see the open branches for the incremental build-out
(chunking, embedders, vector store, reranking, CLI).
