{
  "name": "rag-pipeline-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A small, production-derived retrieval-augmented generation (RAG) pipeline in TypeScript.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "ask": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
